apt update && apt upgrade -y
apt install python3 / sudo apt install python3
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt

next run python3 target.py ,python3 randomsnip.py or python3 targetpost.py
